spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devcourse
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
      generate-ddl: true

jasypt:
  encryptor:
    bean: jasyptStringEncryptor
    password: ${JASYPT_KEY:testKey}

custom:
  base-path:
    image: /Users/juwoongkim/Documents/new/BE-04-Daangn-Market/build/resources/main/static/

jwt:
  secret: secretkey
  tokenValidMillisecond: 3600000
